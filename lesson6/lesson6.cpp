#include <iostream>
using namespace std;
int main() {
    //nhap vao so a, dua ra man hinh gap doi so a
    //input: so nguyen a
    //limit: |a| <= 100
    int a;
    cin >> a;
    cout << "so gap doi cua a la: " << 2 * a;
    return 0;
}
